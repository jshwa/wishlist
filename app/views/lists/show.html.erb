<h1><%= @list.user.username.capitalize %>'s Wishlist</h1

<ul>
  <% @list.gifts.each do |gift| %>
    <%= gift.name %>
  <% end %>
</ul>

<%= link_to "Add a Gift", new_gift_path if @list.user == current_user %>
