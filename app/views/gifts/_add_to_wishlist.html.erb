<% if current_user && current_user.gifts.include?(gift) %>
  <%= button_to "On Your Wishlist", list_path(current_user.list), method: 'get', class: 'card_button'%>
<% else %>
  <%= form_for gift, url: gift_path(gift), html: {method: "patch"} do |g|  %>
    <%= g.submit "Add to Wishlist", class:'card_button' %>
  <% end %> </td>
<% end %>
