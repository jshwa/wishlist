
<%= "Welcome #{current_user.username}" if user_signed_in? %>
<%= link_to "Home", root_path %>
<%= link_to "My Wishlist", list_path(current_user.list)  %>
<%= link_to "Sign In", new_user_session_path if !user_signed_in? %>
<%= link_to "Sign Up", new_user_registration_path if !user_signed_in? %>
<%= link_to "Sign out", destroy_user_session_path, :method => :delete if user_signed_in?%> <br>
